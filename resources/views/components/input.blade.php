<input
    type="{{ $type }}"
    class="form-control {{ $class }}"
    name="{{ $name }}"
    id="{{ $prefix.$name }}"
    placeholder="{{ $caption }}"
    value="{{ $value }}"
    {{ $attributes }}
/>
