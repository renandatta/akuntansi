<div class="custom-control custom-checkbox">
    <input type="checkbox" name="{{ $name }}" class="custom-control-input" id="{{ $prefix.$name }}" {{ $attributes }}>
    <label class="custom-control-label" for="{{ $prefix.$name }}">{{ $caption }}</label>
</div>
